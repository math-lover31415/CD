#include <stdio.h>

/* Multi
Line Comment*/
int main() {
    //Single line comment
    int arr[2] = {1, 2};
    int a,b,c;
    int *p = &a;
    c = a + b;
    a++;
    c += a;
    c = (a && b);
    return 0;
}
